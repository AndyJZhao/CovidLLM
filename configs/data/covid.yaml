# @package _global_


data:
  name: Covid # To be overwritten by dataset specific values.
  alias: Covid
  raw_data_file: data/processed.pkl

  text:
    mode: label_name # How to generate text for each node?
  task_description: >-
    You are an assistant that predicts epidemic trends. You are going to choose the correct answer from several choices of categories: ${data.label_description}
  label_description: '' # To be initialized afterward
  label_text: name
  # * meta_info:
  n_labels: 5
