# @package _global_


data:
  name: Covid # To be overwritten by dataset specific values.
  alias: Covid
  file: data/covid_data.csv

  text:
    mode: label_name # How to generate text for each node?
  task_description: >-
    You are an expert in predicting epidemic trends. You are going to choose the correct answer from several choices of paper categories: ${data.label_description}
  # * meta_info:
  n_labels: 5
